
main :: ()
{
    ////////////////////////////////////////////
    // FOR

    // TODO 
/*
    // Integer range (open end), default step
    for( i := 0..array.count! )
    {

    }

    // Integer range, default step, backwards
    for( i :int = 9..0 )
    {

    }

    // Float range, arbitray step, backwards
    for( t :float = 1 .. .5; t -= 0.1 )
    {

    }

    // Array range, by pointer, conditional steps
    for( p :*b32 = ..hashes; p != null )
    {

    }

    // By item _and_ index
    for( i, it := 0, ..array; i += 1 )
    {

    }

    // By pointer to item and index, range over pointer to array
    for( i, it :int, *int = 0, ..*parray; i += 1 )
    {

    }

    // Loop over infinite range, stop at some arbitrary condition
    for( i := 0..; i < SomeValue() )
    {

    }

    // Infinite loop
    for( .. )
    {
        if( SomeCondition() )
            break;
    }
*/
    // TODO Gather more examples from actual code
}
